data.raw["character"]["character"].light =
    {
      {
        minimum_darkness = 0.3, --0.3Default 0.0
        intensity = 0.5, --Brightness of the light in the range [0, 1], where 0 is no light and 1 is the maximum light.
        size = 100, --60The radius of the light in tiles. Note that the light gets darker near the edges, so the effective size of the light will appear to be smaller.
        --color = {r=1.0, g=1.0, b=1.0}
      }
    }

